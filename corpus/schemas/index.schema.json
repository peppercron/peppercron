{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "description": "Dialect ids, in the order dialect detection tries them. Each id must have a matching dialects/<id>.json.",
  "type": "array",
  "minItems": 1,
  "uniqueItems": true,
  "items": { "type": "string", "pattern": "^[a-z][a-z-]*$" }
}
